# Status Active / Inactive in Workspace

- Change status active / inactive in workspace, reflect in frontend dropdown for workspace, 
option for delete workspace instead of disable. Disable is not required.

- In User tab, disabled user should not be able to login, if user is logged in, then logout user and show message to contact admin.